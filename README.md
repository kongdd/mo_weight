# Fortran weighted module

*   `wsum`: weighted sum
*   `wmean`: weighted mean
    -   `mat`: matrix or vector
    -   `ws`: weights 
    -   `mask`: mask matrix or vector

```
wsum(mat, [ws], [mask])
wmean(mat, [ws], [mask])
```

